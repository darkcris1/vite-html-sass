# Vite + SCSS + UNCSS = "AMAZING"

**Features**

- UNCSS - remove unuse css
- SCSS (autoprefixer)
- Automatic Multipage Entries
- Included (Bootstrap with FONTAWESOME PRO)

## Usage

```bash
pnpm i #or npm i
```

then ...

```bash
pnpm dev #to run in development
```
